import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

// in every react application we have to mout the application on the root element
ReactDOM.render(<App />, document.getElementById('root'));